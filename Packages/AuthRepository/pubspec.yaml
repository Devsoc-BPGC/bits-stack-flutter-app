name: authentication_repository
description: Dart package which manages the authentication domain for the BITSStack App.
version: 1.0.0

publish_to: 'none'

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  equatable: 

dev_dependencies:
  flutter_test:
    sdk: flutter
